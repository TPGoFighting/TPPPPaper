# TPaper 产品需求与系统设计

**日期：** 2026-06-29  
**状态：** 已由用户批准，等待书面复核  
**产品形态：** 单管理员、自托管、Docker Compose 部署  

---

## 1. 产品概述

TPaper 将 PDF、扫描 PDF、DOCX、PNG、JPG 等试卷或复习资料转换为可在线访问的交互式复习网页。系统既能忠实转写已有试卷，也能从普通讲义中生成新的练习题、答案和解析。

生成结果必须先进入管理员草稿。管理员可以修改结构化题目、HTML 模板和 CSS，并分别预览桌面端、平板端和移动端效果。发布时，系统对内容进行校验、净化和编译，生成不可原地修改的发布版本。拿到公开链接的访客无需登录即可答题。

TPaper 不提供源文件下载、页面导出或公开编辑能力。公开网页无法从技术上绝对阻止截图、复制或浏览器保存，因此“不支持下载”的准确含义是：不提供下载入口，不公开源文件，不提供导出接口。

---

## 2. 产品目标

1. 将人工整理试卷网页所需的时间显著缩短。
2. 保留题目、答案、解析、图表和章节之间的语义关系。
3. 让扫描版资料也能通过多模态模型进入统一处理流程。
4. 让管理员在发布前发现并修正 OCR 错字、答案幻觉和布局问题。
5. 让每份资料生成适合手机和桌面使用的交互网页。
6. 支持管理员自定义 API Key、Base URL、文本模型和多模态模型。
7. 保持逻辑与数据、静态资源与动态内容分离。

---

## 3. 非目标 (Out of Scope)

MVP 不包含：

* 多用户注册、租户隔离和团队协作。
* 访客账号、访问密码、付费和订阅。
* 原文件或生成网页下载。
* 排行榜、班级管理、考试监考和证书。
* 移动原生应用。
* 任意模型生成 JavaScript 的执行能力。
* 将所有处理环节拆成独立微服务。

---

## 4. 市场调研结论

* **NotebookLM** 支持基于来源生成闪卡和测验、解释答案并记录学习进度。
* **Quizlet** 支持上传 PDF、DOCX 和 PPTX，并生成可配置题型与长度的练习测试。
* **StudyFetch** 支持多种文档、图片、音视频格式以及 AI 导师、闪卡和测试。
* **Mindgrasp** 支持基于 PDF 生成摘要、闪卡、测验和带来源引用的问答。

这些产品主要将内容留在自有学习平台内。TPaper 的差异化定位是：高保真试卷结构、可编辑的 AI 网页表现层、可公开分享的独立复习页面、自托管，以及 OpenAI-compatible BYOK 模型配置。这是基于上述官方功能描述作出的产品定位推断。

---

## 5. 用户与权限

### 5.1 管理员

系统只有一个管理员。管理员可以：

* 登录管理后台。
* 配置模型服务。
* 上传、解析、重新解析和删除资料。
* 编辑结构化内容、HTML 和 CSS。
* 预览、发布、撤回和创建新版本。
* 查看任务错误、模型调用摘要和源文件剩余保留时间。

### 5.2 访客

访客通过 `/p/{slug}` 访问已发布页面，不需要账号。访客只能读取发布内容、交互答题和查看答案解析，不能调用管理接口或访问草稿与源文件。

---

## 6. 核心用户流程

1. 管理员登录。
2. 上传资料并选择“忠实转写”或“从讲义生成练习”。
3. 后端校验文件并创建异步任务。
4. Worker 提取文字、表格、图片和页面结构。
5. 文本型资料优先使用本地解析；扫描页 and 复杂排版页交给多模态模型。
6. 模型生成结构化 `PaperDocument`。
7. 后端进行 JSON Schema 和语义校验。
8. 模型根据结构化数据生成受控 `presentation.html` 和 `theme.css`。
9. 后端净化 HTML/CSS，并列出可疑内容和低置信度片段。
10. 管理员使用结构化编辑器与 HTML/CSS 编辑器审核。
11. 管理员分别预览桌面、平板和移动端。
12. 发布服务编译、净化并冻结新版本。
13. 访客通过公开链接访问。

---

## 7. 功能需求

### 7.1 管理首页

* 展示资料标题、类型、当前状态、更新时间和公开链接。
* 状态至少包括：上传中、排队中、解析中、模型处理中、待审核、已发布、部分失败、失败。
* 支持按状态筛选和按标题搜索。
* 支持打开草稿、查看发布版本、重新解析、撤回和删除。
* 展示源文件自动删除倒计时。

### 7.2 上传向导

* 支持 PDF、DOCX、PNG、JPG。
* 同时校验扩展名、MIME 和文件签名。
* 可配置文件大小与页数上限；默认上限由部署配置给出，并在上传前显示。
* 允许选择忠实转写或讲义出题模式。
* 讲义出题模式可设置题型、数量、难度、语言和补充要求。
* 提交前显示预计会使用的文本模型或多模态模型。

### 7.3 任务进度

* 按阶段显示进度，而不是伪造连续百分比。
* 显示当前页、已完成页、失败页和重试次数。
* 支持失败页面或失败阶段单独重试。
* 任务刷新页面后仍可继续查看。

### 7.4 审核编辑器

* 结构化编辑题目、选项、正确答案、解析、分值、章节和媒体。
* 支持选择题、判断题、填空题和主观题。
* 支持插入、删除、复制和调整题目顺序。
* 桌面端使用源文件预览、结构化编辑、网页预览的高效率工作区。
* 移动端使用分步页面或标签切换，不展示拥挤的多栏布局。
* 提供 HTML 和 CSS 代码编辑器、错误提示和实时预览。
* 提供 AI 局部修改，修改范围必须由管理员明确选定。
* 低置信度内容和校验错误必须可定位到题目与源页。

### 7.5 发布

* 发布前必须通过结构化校验、组件引用校验、HTML/CSS 安全校验和响应式基础检查。
* 每次发布产生新的不可变 `PublicationVersion`。
* 已发布版本不能原地修改；修改时从某一版本创建新草稿并发布下一版本。
* 公开 slug 默认指向最新有效发布版本。
* 撤回后公开 slug 返回明确的不可用页面，不泄漏草稿。
* 保留发布时间、内容哈希、来源草稿和发布者审计信息。

### 7.6 公开复习页

* 支持答题进度、选择反馈、判断反馈、填空校验、答案展开和回到顶部。
* 题目内容以单列阅读为默认方式。
* 移动端使用大触控目标、吸顶进度和底部题目导航。
* 桌面端可使用目录、章节跳转和更宽的代码/表格区域。
* 访客答题进度默认只保存在浏览器本地，不上传服务器。
* 不提供下载、打印或导出按钮。

### 7.7 模型配置

* 支持多个模型 Profile。
* 每个 Profile 包含名称、协议类型、Base URL、API Key、文本模型、多模态模型、超时、并发和重试配置。
* MVP 首先实现 OpenAI-compatible 协议适配器。
* 支持测试连接、模型列表读取和视觉能力测试；模型服务不支持能力探测时允许管理员手工标注。
* API Key 只在服务端解密使用，前端只能看到掩码。

---

## 8. 响应式设计

### 8.1 移动端

* 目标宽度从 360px 开始。
* 公开页面为单列卡片，无横向溢出。
* 触控目标最小尺寸 44×44px。
* 代码块和复杂表格在自身容器内滚动。
* 管理编辑器使用分步导航，避免同时呈现三个编辑区域。

### 8.2 平板端

* 审核界面允许预览与编辑双栏切换。
* 公开页面保留单列正文，可增加章节侧边抽屉。

### 8.3 桌面端

* 审核界面使用可调整宽度的源文件、编辑器和预览区域。
* 公开页面可显示固定章节导航和居中正文。
* 超宽屏限制正文行宽，避免阅读距离过长。

---

## 9. 系统架构

采用结构化内容与模块化单体方案。Docker Compose 包含：

* **web**：Next.js 管理后台与公开页面。
* **api**：FastAPI 管理、鉴权、编辑、发布和公开读取 API。
* **worker**：文档解析、OCR/多模态、模型生成、净化与清理任务。
* **postgres**：持久化业务数据。
* **redis**：任务队列、锁和短期状态。
* **私有存储卷**：源文件、页面渲染图和处理后的媒体资源。

MVP 使用本地私有存储卷并通过存储接口抽象，未来可以替换为 S3-compatible 存储。源文件与发布媒体使用不同命名空间和访问策略。

---

## 10. 代码与数据边界

### 10.1 静态逻辑

静态逻辑包括管理组件、答题运行时、题型组件、校验器、主题令牌和发布编译器。它们随应用版本发布，不由模型修改。

### 10.2 动态数据

动态数据包括标题、章节、题目、答案、解析、媒体引用、生成配置、HTML 模板 and CSS。动态数据通过版本化模型保存。

### 10.3 AI 生成页面边界

模型生成 `presentation.html` 和 `theme.css`，但不能生成可执行 JavaScript。HTML 使用允许的语义标签和 TPaper 受控组件，例如：

```html
<tp-section source="section-1"> 
  <tp-question source="question-1"></tp-question> 
</tp-section>
```

答题、判分、进度和解析展开由平台运行时实现。模型只决定组合、布局和视觉表现。

---

## 11. 核心数据模型

* **11.1 Paper**：资料的稳定身份，包含标题、slug、模式、状态、当前草稿和当前发布版本引用。
* **11.2 SourceFile**：包含私有存储键、原文件名、MIME、大小、哈希、页数、上传时间和到期时间。默认上传后七天到期，可提前删除。
* **11.3 ProcessingJob**：包含任务类型、阶段、状态、幂等键、进度、错误码、重试次数、模型 Profile 和调用摘要。
* **11.4 PaperDraft**：包含可编辑的 PaperDocument JSON、presentation.html、theme.css、校验结果和草稿版本号。
* **11.5 PublicationVersion**：包含净化并编译后的不可变快照、结构化内容快照、内容哈希、版本号、发布时间和来源草稿版本。
* **11.6 Asset**：包含媒体类型、私有或公开存储键、哈希、尺寸、来源页和替代文本。
* **11.7 ModelProfile**：包含协议、Base URL、加密 API Key、模型名、能力、超时、并发、重试和是否允许访问私有网络。

---

## 12. PaperDocument Schema

Schema 至少包含：

* 文档元数据与语言。
* 章节数组。
* 题目稳定 ID、题号、题型、题干、媒体引用和分值。
* 选择题选项与正确答案。
* 判断题布尔答案。
* 填空题可接受答案与匹配规则。
* 主观题参考答案和评分要点。
* 解析、知识点和来源页。
* 识别置信度与需人工确认标记。
* 语义校验包括：ID 唯一、答案引用有效、必填字段存在、媒体引用有效、分值非负、题型字段匹配。

---

## 13. API 边界

API 按资源分组：

* `/api/auth/*`：管理员登录、登出和会话。
* `/api/model-profiles/*`：模型配置、连接测试和能力检测。
* `/api/uploads/*`：上传初始化、完成和文件状态。
* `/api/papers/*`：资料、草稿和操作。
* `/api/jobs/*`：任务状态、取消和重试。
* `/api/drafts/*`：结构化内容、HTML/CSS 和校验。
* `/api/publications/*`：预检、发布、撤回和版本列表。
* `/api/public/papers/{slug}`：公开不可变内容读取。
* `/api/assets/{id}`：经过访问策略控制的媒体读取。

前后端开发前先冻结 OpenAPI 契约和共享 Schema。不同 Agent 只在契约约束下分别修改前端与后端区域。

---

## 14. AI 处理流水线

* **14.1 预处理**：
  * PDF：提取文本、版面块和图片；文本不足或布局复杂时渲染页面图。
  * DOCX：提取段落、表格、样式层级和媒体，并生成预览。
  * PNG/JPG：直接进入图像预处理和多模态识别。
  * 对页面进行旋转纠正、尺寸限制和清晰度检查。
* **14.2 两阶段模型处理**：
  * 第一阶段只做来源提取，返回页码、文字、布局、媒体关系、置信度和不确定内容。
  * 第二阶段根据模式生成 PaperDocument。忠实转写模式不得擅自补题；讲义出题模式可以生成新题，但必须标记为 AI 生成。
* **14.3 网页生成**：结构化数据通过校验后，网页模型生成受控 HTML 与 CSS。系统将文档内容视为不可信输入，Prompt 明确禁止执行来源文件中的指令。模型输出仍视为不可信，必须经过净化和组件引用校验。
* **14.4 校验和恢复**：
  * JSON 不合法时自动进行一次结构修复。
  * 单页失败时保留其他页面结果。
  * 可重试阶段使用幂等键。
  * 限流和暂时错误使用有上限的指数退避。
  * 永久错误进入人工处理，不进行无限重试。

---

## 15. HTML/CSS 安全模型

* **允许**：安全语义标签、受控 TPaper 组件、class、必要的 data-*、媒体资源 ID 和受限 CSS。
* **禁止**：
  * script、事件处理属性和 javascript: URL。
  * iframe、object、embed、可提交表单。
  * 未经批准的外部脚本、字体、图片和样式地址。
  * CSS 中的远程导入、危险 URL、页面覆盖和越界选择器。
  * 模型直接定义答题或判分逻辑。
  * 所有生成样式必须被限定在发布页面根容器内。发布响应使用严格 CSP。发布预检会向管理员显示被删除或修改的内容。

---

## 16. 安全与隐私

* 管理会话使用 Secure、HttpOnly、SameSite Cookie。
* 所有状态修改接口需要 CSRF 防护。
* 密码使用现代密码哈希算法。
* API Key 使用部署级主密钥进行信封加密；主密钥只存在环境变量或容器 Secret 中。
* 日志对 Key、Authorization、Cookie、Prompt 原文和文件内容进行脱敏。
* 自定义 Base URL 只允许 HTTP/HTTPS；默认阻止回环、链路本地、云元数据和内网地址。
* 连接本地模型需要管理员显式启用私有网络访问，并仍阻止云元数据地址。
* 上传限制类型、大小、频率和解压资源，防止伪造类型与压缩炸弹。
* 源文件无公开路由，七天后由幂等清理任务删除。
* 公开接口只读取已发布快照。

---

## 17. 错误处理

* **文件错误**：拒绝任务并给出用户可理解的错误码和原因。
* **部分解析失败**：保留成功页，失败页可独立重试。
* **模型超时或限流**：显示阶段、重试次数和下一次重试状态。
* **非法 JSON/HTML**：自动修复一次，失败后进入人工处理。
* **安全净化失败**：禁止发布并显示具体问题。
* **发布失败**：保留草稿，现有发布版本不受影响。
* **清理失败**：记录并重试，不误删未到期文件。

---

## 18. 测试策略

* **18.1 单元测试**：
  * PaperDocument Schema 与语义校验。
  * HTML/CSS 净化和恶意输入样例。
  * 模型协议适配器和错误映射。
  * 文件类型检测、到期清理和发布版本冻结。
* **18.2 契约与集成测试**：
  * OpenAPI 前后端契约。
  * API、Worker、PostgreSQL、Redis 和存储卷集成。
  * 文本 PDF、扫描 PDF、DOCX、PNG/JPG 固定样例。
  * 模型返回缺字段、错答案引用和恶意 HTML 的故障注入。
* **18.3 端到端测试**：
  * 登录、上传、处理、编辑、预览、发布和公开答题。
  * 发布版本不可原地修改。
  * 源文件公开访问失败。
  * 桌面、平板、手机关键视口。
  * 键盘操作、焦点、颜色对比和屏幕阅读语义。

---

## 19. MVP 验收标准

1. 支持 PDF、扫描 PDF、DOCX、PNG 和 JPG 进入处理流程。
2. 支持忠实转写和讲义出题两种模式。
3. 支持选择、判断、填空和主观题。
4. 管理员可编辑结构化内容、HTML 和 CSS，并预览三类设备。
5. 模型无法注入或执行 JavaScript。
6. 发布版本不可原地修改，修改必须创建新草稿和新版本。
7. 公开链接无需访客登录，并且不暴露源文件。
8. 源文件默认七天自动清理。
9. 360px、768px 和 1440px 关键视口无页面级横向溢出。
10. 上传至公开答题的核心 E2E 流程通过。
11. 恶意 HTML、提示注入和伪造文件类型测试通过。
12. Docker Compose 可在一台 Linux 服务器上启动完整系统并通过健康检查。

---

## 20. 部署与运维

* 使用 Docker Compose 部署。
* 所有容器提供健康检查。
* 数据库和私有存储使用持久卷。
* 备份包含 PostgreSQL 与发布媒体，不要求备份已到期源文件。
* 日志使用结构化格式，并提供任务 ID 关联。
* 升级前执行数据库备份，迁移保持向后兼容。

---

## 21. 原型与开发顺序

1. 固化 Schema、OpenAPI 和设计令牌。
2. 使用 Figma 与 figma-generate-design 制作管理首页、上传向导、审核编辑器和公开答题页原型。
3. 由用户测试桌面与移动原型并迭代。
4. 使用 Build Web Apps、frontend-design 和 ui-design-aesthetics 开发前端。
5. 使用 web-design-guidelines 做界面与可访问性审核。
6. 在契约稳定后，前端与后端可由不同 Agent 并行开发。
7. 后端接入文件管线、模型适配器、HTML 编译和不可变发布。
8. 完成集成测试、E2E、安全测试和 Docker 部署。

---

## 22. 已确认决策

* MVP 为单管理员自托管系统。
* 公开页面拿到链接即可访问。
* AI 输出必须先审核后发布。
* 支持扫描件 OCR，并优先使用多模态模型。
* 普通讲义可以生成新题、答案和解析。
* 源文件默认保留七天。
* 使用 Docker Compose 部署到 Linux 服务器。
* 采用结构化内容与模块化单体方案。
* 移动端和桌面端分别适配。
